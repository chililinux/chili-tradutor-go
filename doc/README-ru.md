`
#чили-переводчик-гоу 🌶️

chili-translator-go — универсальная оболочка машинного перевода, написанная на Go. Он предназначен для перевода скриптов (.sh, .py), файлов документации (Markdown) и файлов данных (JSON) с сохранением целостности переменных, ссылок и технического синтаксиса.

Его главным преимуществом является Smart Cache v2.1.9, который значительно сокращает сетевые вызовы и ускоряет повторяющиеся переводы за счет повторного использования локальных данных.

## ✨ Особенности

* Мультиформатность: поддерживает .sh, .py, .md, .json, .yaml.
* Сохранение синтаксиса: автоматически защищает переменные оболочки ($VAR, ${VAR}), ссылки Markdown и заполнители строк во время процесса перевода.
* Параллельный перевод: обрабатывает несколько языков одновременно с помощью Goroutines (настраивается через -j).
* Постоянный кеш с меткой времени: локально хранит переводы и управляет жизненным циклом данных, обеспечивая интеллектуальную очистку.
* Прогрессивный интерфейс: отображение прогресса каждого языка в реальном времени с идеальным визуальным выравниванием, независимо от размера кода языка (например, en или zh-CN).

## 🚀 Установка

Убедитесь, что у вас установлен Go и системные зависимости (gettext, trans).
```bash
git клон https://github.com/chililinux/chili-tradutor-go.git
компакт-диск chili-translator-go/src
go build -o chili-translator-go chili-translator-go-v2.1.9.go
sudo mv chili-translator-go /usr/local/bin/
```

## 🛠️ Использование

### Базовый перевод
Чтобы перевести файл на стандартные языки (EN, ES, IT, DE, FR, RU, ZH, JA, KO):

chili-translator-go -i meu_script.sh


### Указание языков и движка

cheli-treducer-go -andtutorial.md


### Очистка кэша
Удалите записи кэша, которые не использовались за последние 30 дней:

chili-translator-go --clean-cache


## ⚙️ Опции (Флаги)

| Флаг | Длинный | Описание |
| :--- | :--- | :--- |
| -я | --входной файл | Исходный файл для перевода. |
| -е | --двигатель | Система перевода: Google, Bing, Яндекс (по умолчанию: Google). |
| -с | --источник | Исходный язык (например: pt, en) (по умолчанию: авто). |
| -л | --язык | Список языков через запятую или все. |
| -j | --вакансии | Количество одновременных переводов (по умолчанию: 8). |
| -ф | --сила | Принудительно выполняет перевод, минуя локальный кеш. |
| | --clean-кэш | Удаляет устаревшие элементы кэша (старше 30 дней). |
| -q | --тихо | Тихий режим (без визуального прогресса). |
| -в | --многословный | Отображает технические подробности во время работы. |
| -В | --версия | Отображает текущую версию. |

## 📁 Структура вывода

* Scripts/POT: генерирует файлы .po в ./pot/ и двоичные файлы .mo в ./usr/share/locale/.
* Markdown: генерирует переведенные версии в ./doc/ (например: README-en.md).
* JSON: генерирует переведенные версии в ./translated/.

## 🛡️ Логика кэша (v2.1.9)

Кэш хранится в ~/.cache/chili-tradutor-go/cache.json.

* Автоматическая миграция: при обнаружении записей из предыдущих версий (v2.1.8) инструмент автоматически ставит текущую временную метку на устаревшие записи, чтобы избежать потери исторических данных.
* Автоматическое обновление: каждый раз, когда элемент находится в кеше, его временная метка «Последнее использование» обновляется, защищая его от автоматической очистки в будущем.
* Безопасность: очистка с помощью --clean-cache удаляет только то, что действительно не используется, обеспечивая здоровый рост вашей базы знаний по переводу.


Разработчик: Вилмар Катафеста <vcatafesta@gmail.com>
Copyright © 2023-2026 Команда ChiliLinux
